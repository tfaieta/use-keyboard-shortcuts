{
  "private": true,
  "workspaces": [
    "example",
    "use-keyboard-shortcuts"
  ],
  "scripts": {
    "build": "yarn build:hook && yarn build:example",
    "build:example": "yarn workspace example package",
    "build:hook": "yarn workspace use-keyboard-shortcuts build",
    "bump": "yarn workspace use-keyboard-shortcuts bump",
    "develop": "yarn watch",
    "watch": "concurrently -n Hook,Example \"yarn watch:hook\" \"yarn watch:example\"",
    "watch:example": "yarn workspace example develop",
    "watch:hook": "yarn workspace use-keyboard-shortcuts watch",
    "setup": "node ./scripts/setup.js"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
